# Threat Model Agent

This folder contains helper assets for the `threat-model` agent.

Usage
- Run the generator locally:

```bash
python3 .github/agents/threat-model/generate_threat_model.py
```

Output
- The script writes a timestamped markdown file into `output/` (ignored by git).
